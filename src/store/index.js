import Vue from "vue";
import Vuex from "vuex";
import atmStore from "@/store/modules/atm-store";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    atmStore
  }
});
